---
description: Update Rust dependencies with per-bump verification and explicit commit approval.
---
Use the `bump-rust-deps` skill to update Rust dependencies with per-bump verification. Obtain explicit user approval immediately before every commit or remote Git action. Additional user instructions: $ARGUMENTS
